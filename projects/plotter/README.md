This is compiled using sigal.  Make sure you're in a virtualenv where sigal is installed (pip install sigal), current version is 1.4.0 at time of writing.  Then type ``sigal build`` to rebuild the HTML.
